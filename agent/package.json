{
  "name": "pod-res-util-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Cursor SDK agent that runs POD resource utilization scripts and analyzes the report",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "agent": "tsx src/index.ts",
    "analyze": "tsx src/index.ts --analyze-only"
  },
  "dependencies": {
    "@cursor/sdk": "^1.0.23"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
